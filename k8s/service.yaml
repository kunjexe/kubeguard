apiVersion: v1
kind: Service
metadata:
  name: kubeguard-service
spec:
  type: NodePort
  selector:
    app: kubeguard
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007